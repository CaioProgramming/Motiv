<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent">

    <fragment
        android:id="@+id/manager_nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/nav_view"
        android:layout_below="@id/motiv_top"
        app:defaultNavHost="true"
        app:navGraph="@navigation/manager_navigation" />

    <include layout="@layout/blur_layout" />

    <include
        android:id="@+id/motiv_top"
        layout="@layout/motiv_manager_toolbar" />

</RelativeLayout>